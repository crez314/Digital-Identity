{
  "name": "@crez/contracts",
  "version": "1.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "jsonschema": "tsx scripts/emit-json-schema.ts",
    "test": "vitest run"
  },
  "dependencies": { "zod": "^3.23.8", "zod-to-json-schema": "^3.23.5" },
  "devDependencies": { "typescript": "^5.6.3", "tsx": "^4.19.2", "vitest": "^2.1.8" }
}
